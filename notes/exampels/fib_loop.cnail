print("numbers that we will calculate:")
for (0, 20 | i: int → nil) {
	print_int(i)
}

print("")
print("fib values")

def fib(x: int) -> int {
	if x == 0 or x == 1 {
		return x
	} else {
		return fib(x - 1) + fib(x - 2)
	}
}
def print_fib(x: int) -> nil {
	print_int(fib(x))
}
for (0, 20, print_fib)
